#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include <time.h>
#include "main.h"

/*bool test_buildArray() {
    char* words[5];
    for(int i=0;i<5;i++)  //loop through 5 for words array
    {
        words[i]=(char*)malloc(5*sizeof(char));  //assighns a new memory block
    }
    bool result=buildWordArray("dictionary.txt",words,5,4);  //calling buildWordArray function from main.c which is called in main.h
    for(int i=0;i<5;i++) 
    {
        free(words[i]);  //freeing the memory of words[i]
    }
    if(result) //starting the condition for the results are passed or fail
    {
        printf("Test case 1: PASSED\n");
    } 
    else 
    {
        printf("Test case 1: FAILED\n");
    }
    return result;
}

bool test_findWord(){
    
}

int main() {
    printf("Testing buildArray()...\n"); 
    if (test_buildArray()) {
        printf("  All tests PASSED!\n");
    } else {
        printf("  test FAILED.\n");
    }

    // printf("Testing totalPVs()...\n"); 
    // if (test_totalPVs()) {
    //     printf("  All tests PASSED!\n");
    // } else {
    //     printf("  test FAILED.\n");
    // }

    return 0;
}*/
